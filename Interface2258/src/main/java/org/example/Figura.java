package org.example;

public interface Figura {
    public double calcularArea();    //como es una declaración abstracta no se pueden crear objetos a partir de esta declaracion
}                                    // porque el compilador no sabría cuantas lineas de codigo reservar para este metodo
